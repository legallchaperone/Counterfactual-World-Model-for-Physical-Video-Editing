# Archived E2W Spec Documents

This directory contains superseded E2W contract, plan, and handoff documents.

They are retained only for historical context. They are not current constraints.

The single current project/runtime spec is:

```text
docs/E2W_SPEC.md
```

If an archived document disagrees with `docs/E2W_SPEC.md`, the current spec wins.
